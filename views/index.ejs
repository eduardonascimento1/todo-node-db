<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo App</title>

  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/neumorphism-ui.css">
</head>
<body class="light-mode">
  <div class="sul-box-raised-2 container">
    <h1>Todo App</h1>
    <!-- Formulário de criação de TODO -->
    <form action="/create" method="POST">
      <input class="sul-text-field" name="title">
      <button class="sul-btn" type="submit">
        Adicionar
      </button>
    </form>
  
    <!-- Exibição da lista de TODOs -->
    <ul>
        <% for (var item of todos) { %>
            <li>
                <% if (item.completed) { %>
                    <s><%= item.title %></s>
                <% } else { %>
                    <form action="/update/<%= item.id %>" method="POST">
                        <input 
                            class="sul-text-field" 
                            name="title" 
                            value="<%= item.title %>"
                        >
                        <button class="sul-btn" type="submit">
                        Editar
                        </button>
                    </form>
                <% } %>
                <a href="/complete/<%= item.id %>">✅</a>
                <a href="/delete/<%= item.id %>">❌</a>
            </li>
        <% } %>
    </ul>
  </div>
</body>
</html>